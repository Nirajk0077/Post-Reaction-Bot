services:
  - type: worker
    name: telegram-reaction-bot
    env: docker
    plan: free # Note: Render Free tier does not support Workers, this might need to be upgraded or changed to a web service with a dummy port if on free tier.
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
